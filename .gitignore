node_modules
.next
out
.vercel
.env*.local

# build artifacts
next-env.d.ts
tsconfig.tsbuildinfo

# tooling state
.impeccable/

# brand + campaign source assets — large binaries, kept on disk only
marketing/
